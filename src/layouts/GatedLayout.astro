---
import Layout from './Layout.astro';
// ContentGate removed to disable auth modal

const { title } = Astro.props;
---

<Layout title={title}>
  <!-- ContentGate removed to allow direct content access -->
  <slot />
</Layout>
